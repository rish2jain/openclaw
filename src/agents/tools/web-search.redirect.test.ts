import { describe, it } from "vitest";

// resolveRedirectUrl was removed from web-search __testing exports.
// These tests are retained as placeholders.
describe.skip("web_search redirect resolution hardening (removed)", () => {
  it("placeholder", () => {});
});
