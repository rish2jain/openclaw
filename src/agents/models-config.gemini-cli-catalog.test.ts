import { describe, it } from "vitest";

// buildGoogleGeminiCliProvider was removed from models-config.providers.
// These tests are retained as placeholders for when the provider is re-introduced.
describe.skip("buildGoogleGeminiCliProvider (removed)", () => {
  it("placeholder", () => {});
});
